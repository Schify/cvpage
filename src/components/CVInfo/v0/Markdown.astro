---
import { md2html } from "../../../utils/cv";
import type { Markdown } from "../../../utils/types";

interface Props {
    data: Markdown;
}

const { data } = Astro.props;
const html = md2html(data.content);
---

<div set:html={html} class={`${data.type}`} />
